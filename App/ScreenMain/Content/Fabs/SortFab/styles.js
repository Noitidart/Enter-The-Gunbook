// @flow

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    picker: {
        position: 'absolute',
        width: '100%',
        height: '100%',
        transform: [{ scale:6 }]
    }
})

export default styles
